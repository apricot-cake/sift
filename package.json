{
  "name": "sift",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:browser": "node scripts/dev-browser.js",
    "build": "wxt build -b chrome && npm run verify:manifest",
    "deploy": "node scripts/deploy-extension.js",
    "test": "node --test test.js",
    "verify:manifest": "node scripts/verify-manifest.js",
    "postinstall": "wxt prepare",
    "prepare": "node scripts/setup.js"
  },
  "devDependencies": {
    "vite": "8.2.0",
    "wxt": "0.21.3"
  }
}
