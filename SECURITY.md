# セキュリティポリシー

## サポート対象

Sift は Chrome ウェブストアで配布していません。利用者はローカルでビルドした拡張機能を読み込みます。セキュリティ修正は `main` に取り込み、サポート対象は最新の `main` だけです。過去のビルドには修正をバックポートしません。

## 脆弱性を報告する

セキュリティ上の問題は、[Report a vulnerability](https://github.com/apricot-cake/sift/security/advisories/new) から非公開で報告してください。

通常の Issue や Pull Request には投稿しないでください。再現手順、実証コード、未修正の脆弱性の詳細は、修正版を公開するまで非公開にしてください。

分かる範囲で、次の情報を添えてください。

- 対象ページのサービス（X、Bluesky、または追加した Misskey インスタンス）
- ビルド元のコミット、OS、Chrome のバージョン
- 攻撃者が得られるものと、必要な権限
- 再現手順

## 対応

報告は draft security advisory 内で確認します。再現を確認して修正し、修正版を利用できる状態にしてから公開方法を調整します。

初動までの時間や修正期限は約束しません。advisory の公開と CVE の申請は、影響に応じて判断します。

## 対象範囲

Sift は、開いている画面に表示された投稿を読み、設定に一致しない投稿を隠します。自動スクロール、バックグラウンドでの収集、非公式 API の呼び出しは行いません。投稿内容を保存・送信せず、Chrome の同期ストレージには利用者の設定値だけを保存します。

投稿内容がブラウザの外部へ送信される、ページが拡張機能の権限を取得する、許可していないサイトで拡張機能が動作する、追加した Misskey ホストへの権限が意図せず広がる、といった問題を特に重視します。

第三者ライブラリの脆弱性は、まずその開発元に報告してください。ライブラリの呼び出し方など Sift 固有の影響がある場合は、こちらにも報告してください。
