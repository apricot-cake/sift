# ブラウザ接続

ブラウザ拡張機能を経由して外部ブラウザへ接続しない。Chrome 拡張機能の実機確認には、開発用プロファイルが公開する CDP 接続だけを使う。

# 拡張機能の配備

拡張機能に対する一連の開発が完了したら、main の主作業ツリーで `npm run deploy` を実行する。このコマンドは Chrome が読む HKCU へ Native Host を登録する。Codex から実行するときは、コマンド全体をユーザー領域への書き込みが許可された実行として承認を得て実行する。実機確認が必要な場合は、配備後に開発用プロファイルへ CDP で接続し、日常用プロファイルと同じ `.output\chrome-mv3` を確認する。
