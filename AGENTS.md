# ブラウザ接続

通常の実機確認には、`npm run dev:browser` で起動した開発用 Chrome のローカル CDP 接続（既定は `http://127.0.0.1:9222`）を使う。日常プロファイル上の動作を検証するときは、日常用 Chrome にブラウザ操作用拡張機能経由で接続してよい。

実機確認では、ページへダミー投稿や検証用 DOM を追加せず、サイトが表示した投稿だけを使う。テスト用 HTML の結果を実機確認として扱わない。

# 拡張機能の変更

拡張機能に対する一連の変更が完了したら、main の主作業ツリーで `npm run deploy` を実行する。Codex から実行するときは、コマンド全体にユーザー領域への書き込みを許可する。実機確認が必要なら、配備後に `npm run verify:browser` を実行する。
