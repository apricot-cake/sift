# ブラウザ接続

実機確認には、`npm run dev:browser` で起動した開発用 Chrome のローカル CDP 接続（ポートは自動取得、接続先は `node scripts/dev-browser.ts --print` で表示）だけを使う。日常プロファイルでの確認や、画面・入力を使う自動操作は、ユーザーが対象と理由を明示した場合に限る。

実機確認では、ページへダミー投稿や検証用 DOM を追加せず、サイトが表示した投稿だけを使う。テスト用 HTML の結果を実機確認として扱わない。

ブラウザ E2E の画面撮影は GitHub Actions の仮想画面で行う。ローカルの画面を撮影や操作の対象にしない。

# 拡張機能の変更

拡張機能に対する一連の変更が完了したら、main の主作業ツリーで `npm run deploy` を実行する。Codex から実行するときは、コマンド全体にユーザー領域への書き込みを許可する。実機確認が必要なら、配備後に `npm run verify:browser` を実行する。
