# 開発版の確認

Chrome 拡張機能の実機確認には、開発用プロファイルが公開する CDP 接続だけを使う。日常用 Chrome にインストールされた ChatGPT 拡張機能を経由しない。

# push 後の完了条件

`main` への直接 push が成功した時点で作業を完了とする。push 後の GitHub Actions は完了を待たず、監視や再取得も行わない。CI 失敗への対応は、ユーザーから別途依頼された場合に行う。

ユーザーが CI の確認、PR のマージ、またはリリースを明示的に依頼した場合だけ、必要なチェックの完了を待つ。
