# ブラウザ接続

ブラウザ拡張機能を経由して外部ブラウザへ接続しない。Chrome 拡張機能の実機確認には、開発用プロファイルが公開する CDP 接続だけを使う。

# 開発サーバー

開発サーバーは Codex の実行セッション内で起動する。利用者に見えるターミナルウィンドウを開かない。画面操作が必要なアプリケーションとブラウザだけをウィンドウとして起動する。

# 拡張機能の配備

拡張機能に対する一連の開発が完了したら、最終検証後に main の主作業ツリーで `npm run deploy` を実行し、日常用 Chrome が読み込むリリースビルドの配置までを完了条件に含める。
